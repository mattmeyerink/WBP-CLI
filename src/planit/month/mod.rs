mod utils;
mod display;
mod data;
mod actions;

pub fn month_view() {
    let mut current_date = chrono::Local::now();

    // Ask the user if they want to enter list view of if they want to enter highlight view
    // Have a loop over the whole thing and a loop each for highlight view and list view
}
